# =================================================================
# 认证配置（Clerk）
# =================================================================
# 重要：该模板支持 Clerk 的无密钥模式！
# 你可以不做任何配置直接启动应用。
# 当你准备认领应用时，点击页面底部的 Clerk 弹窗获取 API Keys。

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# 认证跳转 URL
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/auth/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/auth/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/dashboard/stocks"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/dashboard/stocks"

# =================================================================
# 错误追踪配置（Sentry）
# =================================================================
# 启用 Sentry 错误追踪：
# 1. 在 https://sentry.io 创建账号
# 2. 为 Next.js 创建新项目
# 3. 按下方步骤完成配置

NEXT_PUBLIC_SENTRY_DSN=    # 示例: https://****@****.ingest.sentry.io/****
NEXT_PUBLIC_SENTRY_ORG=    # 示例: acme-corp
NEXT_PUBLIC_SENTRY_PROJECT=    # 示例: nextjs-dashboard
SENTRY_AUTH_TOKEN=    # 示例: sntrys_************************************
NEXT_PUBLIC_SENTRY_DISABLED=false

# =================================================================
# Stock Data API Configuration (Longbridge)
# =================================================================
# Get API credentials from Longbridge User Center:
# https://open.longbridgeapp.com/account
#
# REGION options: US, HK (optional)

LONGPORT_APP_KEY=
LONGPORT_APP_SECRET=
LONGPORT_ACCESS_TOKEN=
LONGPORT_REGION=hk

# =================================================================
# Distributed API Rate Limiting (Upstash Redis)
# =================================================================
# If these are empty, API rate limiting fails open and the in-process
# Longbridge guard remains as a last-mile safety mechanism.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional quota overrides. Defaults are balanced for normal dashboard use.
RATE_LIMIT_ANON_STOCK_READS_PER_MINUTE=120
RATE_LIMIT_AUTH_STOCK_READS_PER_MINUTE=300
RATE_LIMIT_MUTATION_ATTEMPTS_PER_MINUTE=30
RATE_LIMIT_AUTH_MUTATIONS_PER_MINUTE=60
RATE_LIMIT_WS_UPGRADES_PER_MINUTE=30
RATE_LIMIT_WS_MAX_SYMBOLS=30
LONGBRIDGE_QUOTE_BUDGET_PER_MINUTE=600
LONGBRIDGE_KLINE_BUDGET_PER_MINUTE=60
LONGBRIDGE_HEALTH_BUDGET_PER_MINUTE=30
LONGBRIDGE_DAILY_BUDGET=20000

# =================================================================
# Supabase 配置
# =================================================================

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# =================================================================
# 重要说明
# =================================================================
# 1. 本地开发请将该文件重命名为 `.env`
# 2. 不要将真实的 `.env` 提交到版本控制
# 3. 请用真实值替换所有占位符
# 4. 保护好密钥，不要对外泄露
